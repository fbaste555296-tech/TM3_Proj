<x-layouts.app :heading="'Inventory control'"><div class="grid gap-6 xl:grid-cols-3"><div class="card p-6 xl:col-span-2"><h2 class="brand">Parts & accessories</h2><p class="mb-5 text-sm text-slate-500">Low stock is highlighted for faster replenishment.</p><div class="overflow-x-auto"><table class="w-full text-sm"><thead class="border-b text-left text-xs uppercase text-slate-500"><tr><th class="py-3">Item</th><th>SKU</th><th>Category</th><th>On hand</th><th>Unit cost</th></tr></thead><tbody>@forelse($items as $item)<tr class="border-b border-slate-100 {{ $item->isLow()?'bg-rose-50/50':'' }}"><td class="py-4 font-semibold">{{ $item->name }}</td><td>{{ $item->sku }}</td><td>{{ $item->category }}</td><td class="font-bold {{ $item->isLow()?'text-rose-600':'' }}">{{ $item->stock }} <span class="font-normal text-xs text-slate-500">/ min {{ $item->reorder_level }}</span></td><td>₱{{ number_format($item->unit_price,2) }}</td></tr>@empty<tr><td colspan="5" class="py-10 text-center text-slate-500">No parts recorded.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $items->links() }}</div></div><form method="POST" class="card p-6">@csrf<h2 class="brand">Add inventory</h2><p class="mb-4 text-sm text-slate-500">Record a replacement part or accessory.</p><div class="space-y-3"><input class="field" name="sku" placeholder="SKU / item code" required><input class="field" name="name" placeholder="Item name" required><input class="field" name="category" placeholder="Category"><div class="grid grid-cols-2 gap-3"><input class="field" name="stock" type="number" min="0" placeholder="On hand" required><input class="field" name="reorder_level" type="number" min="0" value="5" placeholder="Reorder at" required></div><input class="field" name="unit_price" type="number" step=".01" min="0" placeholder="Unit price" required><button class="w-full rounded-xl bg-emerald-600 py-3 text-sm font-bold text-white">Add item</button></div></form></div></x-layouts.app>
