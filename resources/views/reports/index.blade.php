<x-layouts.app :heading="'Management reports'"><div class="grid gap-4 md:grid-cols-3">@foreach([['Completed jobs',$completed],['Collected revenue','₱'.number_format($totalRevenue,2)],['Outstanding balance','₱'.number_format($outstanding,2)]] as [$name,$number])<div class="card p-5"><p class="label">{{ $name }}</p><p class="brand mt-3 text-3xl">{{ $number }}</p></div>@endforeach</div><div class="mt-7 grid gap-6 lg:grid-cols-2"><div class="card p-6"><h2 class="brand">Service mix</h2><p class="mb-5 text-sm text-slate-500">Jobs by service category.</p>@forelse($byService as $item)<div class="mb-4"><div class="mb-1 flex justify-between text-sm"><span class="font-semibold">{{ $item->service_type }}</span><span>{{ $item->total }} jobs</span></div><div class="h-2 rounded bg-slate-100"><div class="h-2 rounded bg-emerald-500" style="width:{{ min(100,$item->total*12) }}%"></div></div></div>@empty<p class="text-sm text-slate-500">Service data will appear here as jobs are completed.</p>@endforelse</div><div class="card p-6"><h2 class="brand">Commission ledger</h2><p class="mb-5 text-sm text-slate-500">Automatically calculated at 10% of completed job subtotal.</p>@forelse($commissions as $commission)<div class="flex justify-between border-b py-3 text-sm"><div><p class="font-semibold">{{ $commission->technician->name }}</p><p class="text-xs text-slate-500">{{ $commission->job->job_number }} · {{ $commission->status }}</p></div><p class="font-bold">₱{{ number_format($commission->amount,2) }}</p></div>@empty<p class="text-sm text-slate-500">No commissions have been calculated yet.</p>@endforelse</div></div></x-layouts.app>
